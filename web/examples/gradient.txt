def main()
{
  size = 32;
  for(y = 0; y < size; y++){
    for(x = 0; x < size; x++){
        VID_X(x);
        VID_Y(y);
        VID_RED(x);
        VID_GREEN(x+y);
        VID_BLUE(y);
        VID();
    }
  }
}